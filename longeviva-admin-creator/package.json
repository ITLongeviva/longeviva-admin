{
  "name": "create-admin-function",
  "version": "1.0.0",
  "description": "Cloud Function to create a Firebase Admin user",
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "engines": {
    "node": "20"
  }
}
